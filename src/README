nomadCoder강의

package에서 export를 하려면 대문자를 사용해야함

2020-03-17:
NAKED return :  return Value를 명시하지 않아도 코드 명시시

func 함수명( 변수 ) ( 출력값 )
에서 해당되는 값을 반환

defer : 특정 함수를 마무리(return) 했을시 수행하고싶은 작업 명시

함수에 defer 명시후 이후에 실행시킬 내용 명시

반복문 : 오로지 for문만 존재

for문 사용방법

1. for문은 python과 유사하며 range를 활용하여 범위를 지정함

for index , number := range numbers {
    이런식으로 쓸수있음
}

2. 익숙한 방법

for i := 0 ; i < len (numbers) ; i += 1 {
}

for (;;)서 소괄호만 사라짐

//

2020-03-18

switch문 => c나 js랑 거의 유사함 차이점은

대상 변수를 :=로 변경 가능함

ex ) koreanAge := age + 2 이런식으로

map => 직관적이진 않았다. JS의 object 와 비슷한데 선언의 경우

	nico := map[string]string{"name" : "nico", "age" : "27"}
등 해당 키의 type을 , 내용물의 type을 모두 정하고 시작

Structs

고 언어에서는 constructor 이 존재하지 않음

이론 종료

이후로 dicitionary 프로젝트